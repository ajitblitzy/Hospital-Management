# Dependencies
node_modules/

# Playwright reports & test artifacts (regenerated every run)
playwright-report/
test-results/
blob-report/
playwright/.cache/

# Cached authentication storage state (seeded creds — never commit)
.auth/

# Local environment overrides (keep the tracked template)
.env
.env.*
!.env.example

# Logs & OS/editor cruft
*.log
.DS_Store
Thumbs.db
