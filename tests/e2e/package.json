{
  "name": "@hms/e2e-tests",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end UI automation (Playwright) for the Hospital Management System — real user journeys across all 8 roles and core modules against the running React frontend.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "show-report": "playwright show-report",
    "install:browsers": "playwright install"
  },
  "dependencies": {
    "@hms/test-fixtures": "file:../fixtures"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5"
  },
  "keywords": [
    "hms",
    "e2e",
    "playwright",
    "ui-automation",
    "testing"
  ]
}
